import {Download,ShieldCheck} from 'lucide-react'; import {Card,CardContent} from '../components/ui/Card'; import {Button} from '../components/ui/Button'; import type {Certificate,EwasteItem,Pickup,Recycler} from '../lib/types';
export function CertificatePage({certificate,item,pickup,recycler}:{certificate:Certificate;item:EwasteItem;pickup:Pickup;recycler:Recycler}){function print(){window.print()}return <div className="fade-in mx-auto max-w-3xl"><div className="mb-6 flex items-center justify-between"><div><p className="text-sm font-semibold text-leaf">Completed</p><h1 className="mt-1 text-3xl font-semibold">Disposal certificate</h1></div><Button variant="secondary" onClick={print}><Download size={16}/>Download / Print</Button></div><Card className="overflow-hidden" id="certificate"><div className="bg-ink p-8 text-white"><div className="flex items-center gap-3"><div className="grid h-12 w-12 place-items-center rounded-xl bg-white/10"><ShieldCheck/></div><div><p className="text-xs font-semibold uppercase tracking-[.2em] text-white/60">EcoLoop</p><h2 className="mt-1 text-2xl font-semibold">RESPONSIBLE DISPOSAL CERTIFICATE</h2></div></div></div><CardContent className="space-y-7 p-8"><p className="text-lg leading-8 text-slate-700">This certificate records that the listed item completed the EcoLoop demo disposal lifecycle.</p><div className="grid gap-5 sm:grid-cols-2">{[['Certificate ID',certificate.number],['Item',item.itemType],['Quantity',String(pickup.quantity)],['Recycler',recycler.name],['Collection date',pickup.date],['Completion date',new Date(certificate.issuedAt).toLocaleDateString()]].map(([k,v])=><div key={k}><p className="text-xs font-semibold uppercase tracking-wide text-slate-400">{k}</p><p className="mt-1 font-semibold">{v}</p></div>)}</div><div className="border-t border-line pt-6"><p className="font-semibold">Final status</p><p className="mt-1 text-leaf">Responsibly recycled</p><p className="mt-5 text-sm text-slate-500">Responsibly recycled through the EcoLoop network.</p></div></CardContent></Card></div>}
