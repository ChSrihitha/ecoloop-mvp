import {Award,CheckCircle2,Download,Leaf,Recycle,Share2} from 'lucide-react';
import {Card,CardContent} from '../components/ui/Card';
import {Button} from '../components/ui/Button';
import {PageHeader} from '../components/ui/Structure';
import type {Certificate,EwasteItem,Pickup,Recycler} from '../lib/types';

export function CertificatePage({certificate,item,pickup,recycler}:{certificate:Certificate;item:EwasteItem;pickup:Pickup;recycler:Recycler}){return <div className="fade-in mx-auto max-w-3xl space-y-8"><PageHeader eyebrow="Completion" title="Responsibly recycled" subtitle="A clear record of the handoff is ready to keep." actions={<div className="flex gap-2"><Button variant="secondary"><Download size={16}/>Download</Button><Button variant="ghost"><Share2 size={16}/>Share</Button></div>}/><Card className="overflow-hidden border-leaf/25"><div className="bg-ink p-8 text-center text-white md:p-12"><div className="mx-auto grid h-16 w-16 place-items-center rounded-2xl bg-white/10"><Award size={31} className="text-mint"/></div><p className="mt-5 text-xs font-bold uppercase tracking-[.2em] text-mint">EcoLoop responsibility certificate</p><h1 className="mt-3 text-3xl font-semibold tracking-tight">A better destination, completed.</h1><p className="mt-3 text-sm text-white/60">Certificate {certificate.number}</p></div><CardContent className="space-y-7 md:p-8"><div className="grid gap-4 sm:grid-cols-2"><Info label="Item" value={item.itemType}/><Info label="Recycler" value={recycler.name}/><Info label="Pickup date" value={`${pickup.date} · ${pickup.time}`}/><Info label="Issued" value={new Date(certificate.issuedAt).toLocaleDateString()}/></div><div className="flex items-start gap-3 rounded-2xl bg-[#edf6ef] p-5"><CheckCircle2 className="mt-0.5 shrink-0 text-leaf" size={21}/><div><p className="font-semibold">Responsibly recycled</p><p className="mt-1 text-sm leading-6 text-slate-600">This item has completed the EcoLoop disposal journey with the selected recycler.</p></div></div><div className="grid gap-4 border-t border-line pt-6 sm:grid-cols-3"><Metric icon={Recycle} label="Handoff" value="Completed"/><Metric icon={Leaf} label="Impact" value="Recorded"/><Metric icon={Award} label="Status" value="Verified"/></div></CardContent></Card></div>}
function Info({label,value}:{label:string;value:string}){return <div><p className="text-xs font-bold uppercase tracking-[.14em] text-slate-400">{label}</p><p className="mt-2 font-semibold">{value}</p></div>}
function Metric({icon:Icon,label,value}:{icon:React.ElementType;label:string;value:string}){return <div className="flex items-center gap-3"><Icon size={18} className="text-leaf"/><div><p className="text-xs text-slate-400">{label}</p><p className="mt-1 text-sm font-semibold">{value}</p></div></div>}