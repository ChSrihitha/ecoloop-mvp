import {MapPin,Star,ShieldCheck,Truck,ArrowRight} from 'lucide-react'; import {recyclers} from '../lib/demoData'; import {Card,CardContent} from '../components/ui/Card'; import {Badge} from '../components/ui/Badge'; import {Button} from '../components/ui/Button'; import type {ItemType,Recycler} from '../lib/types';
export function Recyclers({itemType,onSelect}:{itemType:ItemType;onSelect:(r:Recycler)=>void}){const ranked=[...recyclers].sort((a,b)=>Number(b.acceptedItems.includes(itemType))-Number(a.acceptedItems.includes(itemType)));return <div className="fade-in mx-auto max-w-4xl"><p className="text-sm font-semibold text-leaf">Step 3 of 4</p><h1 className="mt-1 text-3xl font-semibold">Find a recycler</h1><p className="mt-2 text-slate-500">Demo records are clearly labeled; connect your own verified recycler data before production.</p><div className="mt-7 space-y-4">{ranked.map((r,i)=><Card key={r.id} className={i===0?'ring-2 ring-ink':''}><CardContent><div className="flex flex-col gap-5 md:flex-row md:items-center md:justify-between"><div className="flex gap-4"><div className="grid h-12 w-12 shrink-0 place-items-center rounded-xl bg-mint"><ShieldCheck size={21}/></div><div><div className="flex flex-wrap items-center gap-2"><h2 className="font-semibold">{r.name}</h2>{i===0&&<Badge>Recommended for you</Badge>}</div><p className="mt-1 flex items-center gap-1 text-sm text-slate-500"><MapPin size={14}/>{r.location}</p><div className="mt-3 flex flex-wrap gap-2 text-xs text-slate-600"><span className="rounded-full bg-cream px-2.5 py-1">{r.certificationStatus}</span><span className="rounded-full bg-cream px-2.5 py-1"><Star size={12} className="mr-1 inline"/>{r.rating}</span><span className="rounded-full bg-cream px-2.5 py-1"><Truck size={12} className="mr-1 inline"/>{r.pickupAvailability}</span></div></div></div><div className="md:w-52">{i===0&&<p className="mb-2 text-xs text-slate-500">{r.acceptedItems.includes(itemType)?`Recommended because this recycler accepts ${itemType.toLowerCase()} and supports pickup.`:'Best available demo match based on current seeded data.'}</p>}<Button className="w-full" onClick={()=>onSelect(r)}>Select Recycler <ArrowRight size={16}/></Button></div></div><div className="mt-4 border-t border-line pt-3 text-xs text-slate-500">Accepted: {r.acceptedItems.join(' · ')}</div></CardContent></Card>)}</div></div>}
