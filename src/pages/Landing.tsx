import {ArrowRight,CheckCircle2,Recycle} from 'lucide-react';
import {Button} from '../components/ui/Button';
import {Card,CardContent} from '../components/ui/Card';

export function Landing({onNavigate}:{onNavigate:(page:'signin'|'signup'|'dashboard')=>void}){return <div className="min-h-screen bg-cream"><header className="border-b border-line bg-cream"><div className="mx-auto flex max-w-6xl items-center justify-between px-5 py-4"><button onClick={()=>onNavigate('dashboard')} className="flex items-center gap-2 font-bold tracking-tight"><span className="grid h-9 w-9 place-items-center rounded-xl bg-ink text-white"><Recycle size={19}/></span>EcoLoop</button><nav className="flex items-center gap-2"><Button variant="ghost" onClick={()=>onNavigate('signin')}>Sign in</Button><Button onClick={()=>onNavigate('signup')}>Get started</Button></nav></div></header><main><section className="mx-auto grid max-w-6xl gap-8 px-5 py-16 md:grid-cols-[1.1fr_.9fr] md:items-center md:py-24"><div><p className="text-sm font-semibold text-leaf">Responsible e-waste, end to end</p><h1 className="mt-3 max-w-2xl text-4xl font-semibold tracking-tight md:text-6xl">Recycle electronics with confidence.</h1><p className="mt-5 max-w-xl text-lg leading-8 text-slate-600">EcoLoop helps you identify e-waste, find a suitable recycler, schedule a pickup, and keep a record of the impact.</p><div className="mt-8 flex flex-wrap gap-3"><Button onClick={()=>onNavigate('signup')}>Start recycling <ArrowRight size={16}/></Button><Button variant="secondary" onClick={()=>onNavigate('signin')}>Sign in</Button></div></div><Card><CardContent className="space-y-5"><p className="text-sm font-semibold">One clear recycling workflow</p>{['Add your e-waste','Review the analysis','Choose a recycler','Schedule a pickup'].map(step=><div key={step} className="flex items-center gap-3 text-sm"><CheckCircle2 size={18} className="text-leaf"/>{step}</div>)}</CardContent></Card></section><section className="border-y border-line bg-white"><div className="mx-auto max-w-6xl px-5 py-14"><h2 className="text-2xl font-semibold">How it works</h2><div className="mt-7 grid gap-4 md:grid-cols-3">{[['01','Identify','Add an item and a photo so the workflow has the right context.'],['02','Match','Compare recycler options available for your item.'],['03','Track','Schedule a handoff and follow its progress to responsible disposal.']].map(([number,title,description])=><div key={number}><p className="text-sm font-semibold text-leaf">{number}</p><h3 className="mt-2 font-semibold">{title}</h3><p className="mt-2 text-sm leading-6 text-slate-500">{description}</p></div>)}</div></div></section><section className="mx-auto max-w-6xl px-5 py-14"><div className="grid gap-4 md:grid-cols-3">{[['Less waste','Keep electronics out of informal disposal streams.'],['Clear choices','See recycler details before you schedule.'],['Visible impact','Keep a history of what you diverted.']].map(([title,description])=><Card key={title}><CardContent><h3 className="font-semibold">{title}</h3><p className="mt-2 text-sm leading-6 text-slate-500">{description}</p></CardContent></Card>)}</div></section><section className="border-t border-line bg-ink px-5 py-14 text-center text-white"><h2 className="text-2xl font-semibold">Ready to make your next device count?</h2><Button className="mt-5 bg-white text-ink hover:bg-white/90" onClick={()=>onNavigate('signup')}>Get started <ArrowRight size={16}/></Button></section></main><footer className="mx-auto max-w-6xl px-5 py-6 text-sm text-slate-500">EcoLoop · E-waste pickup coordination</footer></div>}
