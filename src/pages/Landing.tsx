import {ArrowRight,Battery,ChevronDown,Leaf,Laptop,Menu,Recycle,ShieldCheck,Smartphone,X} from 'lucide-react';
import {useState} from 'react';
import {Button} from '../components/ui/Button';
import {HowItWorks} from '../components/HowItWorks';
import {LandingFooter} from '../components/LandingFooter';
import {LandingImpact} from '../components/LandingImpact';

type LandingPage='signin'|'signup'|'dashboard';
const navLinks=[['How it works','how-it-works'],['For recyclers','for-recyclers'],['Impact','impact']] as const;

export function Landing({onNavigate}:{onNavigate:(page:LandingPage)=>void}){const [menuOpen,setMenuOpen]=useState(false);const scrollTo=(id:string)=>{document.getElementById(id)?.scrollIntoView({behavior:'smooth'});setMenuOpen(false)};return <div className="min-h-screen overflow-hidden bg-cream"><header className="sticky top-0 z-30 px-3 pt-3 sm:px-5"><div className="mx-auto flex max-w-6xl items-center justify-between rounded-2xl border border-line/90 bg-cream/90 px-3 py-2 shadow-[0_8px_24px_rgba(16,37,29,.05)] backdrop-blur md:px-4"><button onClick={()=>onNavigate('dashboard')} className="flex shrink-0 items-center gap-2 px-2 py-1 font-bold tracking-tight"><span className="grid h-8 w-8 place-items-center rounded-xl bg-ink text-white"><Recycle size={17}/></span>EcoLoop</button><nav className="hidden items-center gap-1 rounded-xl border border-line/80 bg-white/55 p-1 lg:flex">{navLinks.map(([label,id])=><button key={id} onClick={()=>scrollTo(id)} className="rounded-lg px-3 py-2 text-sm font-medium text-slate-600 transition hover:bg-white hover:text-ink">{label}</button>)}</nav><div className="hidden items-center gap-2 sm:flex"><Button variant="ghost" onClick={()=>onNavigate('signin')}>Sign in</Button><Button onClick={()=>onNavigate('dashboard')}>Recycle an item <ArrowRight size={15}/></Button></div><button aria-label="Toggle navigation" onClick={()=>setMenuOpen(!menuOpen)} className="grid h-10 w-10 place-items-center rounded-xl border border-line bg-white sm:hidden">{menuOpen?<X size={19}/>:<Menu size={19}/>}</button></div>{menuOpen&&<div className="mx-auto mt-2 max-w-6xl rounded-2xl border border-line bg-cream p-2 shadow-soft sm:hidden"><div className="grid gap-1">{navLinks.map(([label,id])=><button key={id} onClick={()=>scrollTo(id)} className="flex items-center justify-between rounded-xl px-4 py-3 text-left text-sm font-medium text-slate-600 hover:bg-white">{label}<ChevronDown size={15} className="-rotate-90"/></button>)}</div><div className="mt-2 grid gap-2 border-t border-line p-2 pt-3"><Button variant="ghost" onClick={()=>{setMenuOpen(false);onNavigate('signin')}}>Sign in</Button><Button onClick={()=>{setMenuOpen(false);onNavigate('dashboard')}}>Recycle an item <ArrowRight size={15}/></Button></div></div>}</header><main><section className="relative isolate border-b border-line"><div className="pointer-events-none absolute inset-0 -z-10 bg-[radial-gradient(circle_at_75%_20%,rgba(47,125,85,.11),transparent_34%)]"/><div className="mx-auto grid min-h-[calc(100vh-76px)] max-w-6xl items-center gap-14 px-5 py-16 md:grid-cols-[1.02fr_.98fr] md:py-20"><div className="reveal"><p className="text-xs font-bold tracking-[.2em] text-leaf">RESPONSIBLE E-WASTE, END TO END</p><h1 className="mt-5 max-w-2xl text-5xl font-semibold leading-[1.02] tracking-tight md:text-7xl">Give your electronics a responsible second life.</h1><p className="mt-6 max-w-xl text-base leading-7 text-slate-600 md:text-lg">Identify your e-waste, understand how it should be handled, find a suitable certified recycler, schedule a pickup, and track responsible disposal — all in one place.</p><div className="mt-8 flex flex-wrap items-center gap-3"><Button onClick={()=>onNavigate('dashboard')}>Recycle an item <ArrowRight size={16}/></Button><button onClick={()=>scrollTo('how-it-works')} className="inline-flex items-center gap-2 rounded-xl px-4 py-2.5 text-sm font-semibold text-ink hover:bg-white">See how it works <ArrowRight size={16}/></button></div><div className="mt-10 flex items-center gap-3 text-sm text-slate-500"><div className="flex -space-x-2"><span className="grid h-8 w-8 place-items-center rounded-full border-2 border-cream bg-mint text-leaf"><Recycle size={14}/></span><span className="grid h-8 w-8 place-items-center rounded-full border-2 border-cream bg-ink text-white"><ShieldCheck size={14}/></span><span className="grid h-8 w-8 place-items-center rounded-full border-2 border-cream bg-[#e5eadf] text-ink"><Leaf size={14}/></span></div>Built around responsible handoffs</div></div><div className="reveal reveal-delay-2 relative mx-auto w-full max-w-md"><div className="relative aspect-[.9] overflow-hidden rounded-[2rem] border border-ink/10 bg-[#e7efe8] p-5 shadow-soft"><div className="absolute inset-5 rounded-[1.5rem] border border-leaf/20"/><div className="absolute inset-x-10 top-10 h-32 rounded-full bg-leaf/10 blur-3xl"/><div className="absolute left-10 top-12 text-leaf"><Smartphone size={42} strokeWidth={1.2}/></div><div className="absolute right-10 top-20 text-ink/75"><Laptop size={54} strokeWidth={1.1}/></div><div className="absolute bottom-16 left-16 text-leaf/80"><Battery size={38} strokeWidth={1.2}/></div><div className="absolute bottom-14 right-14 text-ink"><Recycle size={48} strokeWidth={1.1}/></div><div className="absolute left-1/2 top-1/2 grid h-32 w-32 -translate-x-1/2 -translate-y-1/2 place-items-center rounded-full border border-ink/15 bg-cream/90 shadow-[0_12px_35px_rgba(16,37,29,.1)]"><Recycle size={52} strokeWidth={1.1} className="text-ink"/></div><div className="absolute bottom-5 left-1/2 flex -translate-x-1/2 items-center gap-2 whitespace-nowrap rounded-full border border-ink/10 bg-white/80 px-3 py-2 text-xs font-semibold text-ink"><ShieldCheck size={15} className="text-leaf"/>Certified recycler network</div></div><div className="absolute -right-2 top-8 hidden items-center gap-2 rounded-xl border border-line bg-white px-3 py-2 text-xs font-semibold shadow-soft sm:flex"><span className="h-2 w-2 rounded-full bg-leaf"/>A better route for every device</div></div></div></section><HowItWorks/><section id="for-recyclers" className="scroll-mt-24 border-b border-line"><div className="mx-auto grid max-w-6xl gap-12 px-5 py-20 md:grid-cols-[.8fr_1.2fr] md:items-end"><div><p className="text-xs font-bold tracking-[.18em] text-leaf">WHY ECOLOOP</p><h2 className="mt-3 text-3xl font-semibold tracking-tight md:text-4xl">The responsible choice should be the easy choice.</h2></div><div className="grid gap-3 sm:grid-cols-2">{[['Responsible disposal','Keep electronics out of informal disposal streams.'],['Certified matching','Choose from recycler records built for trust.'],['Pickup coordination','Turn a good intention into a completed handoff.'],['Visible impact','See what your responsible action adds up to.']].map(([title,description])=><div key={title} className="border-t border-line py-4"><h3 className="font-semibold">{title}</h3><p className="mt-1 text-sm leading-6 text-slate-500">{description}</p></div>)}</div></div></section><LandingImpact onStart={()=>onNavigate('dashboard')}/></main><LandingFooter onNavigate={onNavigate} onScroll={scrollTo}/></div>}