import {ArrowRight,PackageCheck} from 'lucide-react'; import {Card,CardContent} from '../components/ui/Card'; import type {Store} from '../lib/storage';
export function Pickups({store,onTrack}:{store:Store;onTrack:(id:string)=>void}){return <div className="fade-in"><div><p className="text-sm font-semibold text-leaf">My Pickups</p><h1 className="mt-1 text-3xl font-semibold">Disposal activity</h1></div><div className="mt-7 space-y-3">{store.pickups.length===0?<Card><CardContent className="py-12 text-center"><PackageCheck className="mx-auto text-slate-300" size={32}/><p className="mt-3 font-medium">No pickups scheduled yet.</p></CardContent></Card>:store.pickups.slice().reverse().map(p=><Card key={p.id}><CardContent className="flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between"><div><p className="font-semibold">Pickup {p.id.slice(-8).toUpperCase()}</p><p className="mt-1 text-sm text-slate-500">{p.date} · {p.time} · {p.address}</p></div><button onClick={()=>onTrack(p.id)} className="flex items-center gap-2 text-sm font-semibold text-leaf">Track <ArrowRight size={16}/></button></CardContent></Card>)}</div></div>}
