import {ArrowRight,CalendarDays,PackageCheck} from 'lucide-react';
import {Card,CardContent} from '../components/ui/Card';
import {PageHeader,StatusBadge,EmptyState} from '../components/ui/Structure';
import type {Store} from '../lib/storage';

export function Pickups({store,onTrack}:{store:Store;onTrack:(id:string)=>void}){return <div className="fade-in mx-auto max-w-4xl space-y-8"><PageHeader eyebrow="Your activity" title="My pickups" subtitle="Every scheduled handoff, in one clear record."/><div className="space-y-3">{store.pickups.length===0?<EmptyState title="No pickups scheduled yet" description="Your next responsible handoff will appear here."/>:store.pickups.slice().reverse().map(p=><Card key={p.id}><CardContent className="flex flex-col gap-5 sm:flex-row sm:items-center sm:justify-between"><div className="flex items-start gap-4"><div className="grid h-11 w-11 shrink-0 place-items-center rounded-xl bg-mint text-leaf"><PackageCheck size={20}/></div><div><div className="flex flex-wrap items-center gap-2"><p className="font-semibold">Pickup {p.id.slice(-8).toUpperCase()}</p><StatusBadge status={p.status}/></div><p className="mt-2 flex items-center gap-2 text-sm text-slate-500"><CalendarDays size={14}/>{p.date} · {p.time}</p><p className="mt-1 text-sm text-slate-500">{p.address}</p></div></div><button onClick={()=>onTrack(p.id)} className="flex items-center gap-2 text-sm font-semibold text-leaf sm:shrink-0">Track journey <ArrowRight size={16}/></button></CardContent></Card>)}</div></div>}