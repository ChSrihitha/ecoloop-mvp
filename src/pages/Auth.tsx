import {ArrowLeft,Recycle} from 'lucide-react';
import {Button} from '../components/ui/Button';
import {Card,CardContent} from '../components/ui/Card';

export function Auth({mode,onBack,onComplete,onSwitch}:{mode:'signin'|'signup';onBack:()=>void;onComplete:()=>void;onSwitch:(mode:'signin'|'signup')=>void}){const signup=mode==='signup';return <div className="min-h-screen bg-cream px-5 py-8"><div className="mx-auto max-w-md"><button onClick={onBack} className="mb-8 flex items-center gap-2 text-sm font-semibold"><ArrowLeft size={16}/>Back to EcoLoop</button><div className="mb-6 flex items-center gap-2 font-bold"><span className="grid h-9 w-9 place-items-center rounded-xl bg-ink text-white"><Recycle size={19}/></span>EcoLoop</div><Card><CardContent className="space-y-5"><div><h1 className="text-2xl font-semibold">{signup?'Create your account':'Welcome back'}</h1><p className="mt-2 text-sm text-slate-500">{signup?'Set up your EcoLoop workspace.':'Continue to your recycling workspace.'}</p></div>{signup&&<label className="block"><span className="mb-2 block text-sm font-semibold">Name</span><input className="w-full rounded-xl border border-line px-4 py-3" placeholder="Your name"/></label>}<label className="block"><span className="mb-2 block text-sm font-semibold">Email</span><input type="email" className="w-full rounded-xl border border-line px-4 py-3" placeholder="you@example.com"/></label><label className="block"><span className="mb-2 block text-sm font-semibold">Password</span><input type="password" className="w-full rounded-xl border border-line px-4 py-3" placeholder="Password"/></label><Button className="w-full" onClick={onComplete}>{signup?'Create account':'Sign in'}</Button><p className="text-center text-sm text-slate-500">{signup?'Already have an account?':'Need an account?'} <button className="font-semibold text-ink underline" onClick={()=>onSwitch(signup?'signin':'signup')}>{signup?'Sign in':'Sign up'}</button></p></CardContent></Card></div></div>}
