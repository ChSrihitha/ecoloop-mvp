import {useState} from 'react'; import {CalendarDays,MapPin,Phone,ArrowRight} from 'lucide-react'; import {Card,CardContent} from '../components/ui/Card'; import {Button} from '../components/ui/Button'; import type {ItemType,Recycler} from '../lib/types';
export function Schedule({recycler,itemType,onBooked}:{recycler:Recycler;itemType:ItemType;onBooked:(v:{address:string;date:string;time:string;quantity:number;contact:string})=>void}){const [address,setAddress]=useState('');const [date,setDate]=useState('');const [time,setTime]=useState('10:00–12:00');const [quantity,setQuantity]=useState(1);const [contact,setContact]=useState('');return <div className="fade-in mx-auto max-w-3xl"><p className="text-sm font-semibold text-leaf">Step 4 of 4</p><h1 className="mt-1 text-3xl font-semibold">Schedule pickup</h1><p className="mt-2 text-slate-500">Choose a convenient handoff window for your {itemType.toLowerCase()}.</p><Card className="mt-7"><CardContent className="space-y-5"><div className="rounded-xl bg-cream p-4"><p className="text-xs font-semibold uppercase tracking-wide text-slate-400">Selected recycler</p><p className="mt-1 font-semibold">{recycler.name}</p><p className="text-sm text-slate-500">{recycler.location}</p></div><label className="block"><span className="mb-2 block text-sm font-semibold">Pickup address</span><div className="relative"><MapPin className="absolute left-3 top-3.5 text-slate-400" size={17}/><input value={address} onChange={e=>setAddress(e.target.value)} className="w-full rounded-xl border border-line px-10 py-3 outline-none focus:ring-2 focus:ring-leaf/20" placeholder="House / apartment / landmark"/></div></label><div className="grid gap-4 sm:grid-cols-2"><label><span className="mb-2 block text-sm font-semibold">Date</span><div className="relative"><CalendarDays className="absolute left-3 top-3.5 text-slate-400" size={17}/><input type="date" value={date} onChange={e=>setDate(e.target.value)} className="w-full rounded-xl border border-line px-10 py-3"/></div></label><label><span className="mb-2 block text-sm font-semibold">Time slot</span><select value={time} onChange={e=>setTime(e.target.value)} className="w-full rounded-xl border border-line px-4 py-3"><option>10:00–12:00</option><option>12:00–14:00</option><option>16:00–18:00</option></select></label></div><div className="grid gap-4 sm:grid-cols-2"><label><span className="mb-2 block text-sm font-semibold">Quantity</span><input type="number" min={1} value={quantity} onChange={e=>setQuantity(Math.max(1,Number(e.target.value)))} className="w-full rounded-xl border border-line px-4 py-3"/></label><label><span className="mb-2 block text-sm font-semibold">Contact</span><div className="relative"><Phone className="absolute left-3 top-3.5 text-slate-400" size={17}/><input value={contact} onChange={e=>setContact(e.target.value)} className="w-full rounded-xl border border-line px-10 py-3" placeholder="Phone or email"/></div></label></div><Button disabled={!address||!date||!contact} className="w-full py-3.5" onClick={()=>onBooked({address,date,time,quantity,contact})}>Schedule Pickup <ArrowRight size={17}/></Button></CardContent></Card></div>}
