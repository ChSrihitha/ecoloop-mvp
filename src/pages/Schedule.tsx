import {useState} from 'react';
import {ArrowRight,CalendarDays,CheckCircle2,MapPin,Phone} from 'lucide-react';
import {Card,CardContent} from '../components/ui/Card';
import {Button} from '../components/ui/Button';
import {PageHeader,StepIndicator} from '../components/ui/Structure';
import type {ItemType,Recycler} from '../lib/types';

export function Schedule({recycler,itemType,onBooked}:{recycler:Recycler;itemType:ItemType;onBooked:(v:{address:string;date:string;time:string;quantity:number;contact:string})=>void}){const [address,setAddress]=useState('');const [date,setDate]=useState('');const [time,setTime]=useState('10:00–12:00');const [quantity,setQuantity]=useState(1);const [contact,setContact]=useState('');return <div className="fade-in mx-auto max-w-4xl space-y-8"><PageHeader eyebrow="Step 4 of 4" title="Schedule your pickup" subtitle={`Choose a convenient handoff window for your ${itemType.toLowerCase()}.`}/><StepIndicator current={3} steps={['Add e-waste','AI analysis','Find recycler','Schedule pickup']}/><div className="grid gap-5 lg:grid-cols-[1fr_.7fr]"><Card><CardContent className="space-y-6 md:p-8"><label className="block"><span className="mb-2 block text-sm font-semibold">Pickup address</span><div className="relative"><MapPin className="absolute left-3 top-3.5 text-slate-400" size={17}/><input value={address} onChange={e=>setAddress(e.target.value)} className="w-full rounded-xl border border-line bg-cream px-10 py-3 outline-none focus:border-leaf" placeholder="House, apartment, or landmark"/></div></label><div className="grid gap-4 sm:grid-cols-2"><label><span className="mb-2 block text-sm font-semibold">Date</span><div className="relative"><CalendarDays className="absolute left-3 top-3.5 text-slate-400" size={17}/><input type="date" value={date} onChange={e=>setDate(e.target.value)} className="w-full rounded-xl border border-line bg-cream px-10 py-3 outline-none focus:border-leaf"/></div></label><label><span className="mb-2 block text-sm font-semibold">Time slot</span><select value={time} onChange={e=>setTime(e.target.value)} className="w-full rounded-xl border border-line bg-cream px-4 py-3 outline-none focus:border-leaf"><option>10:00–12:00</option><option>12:00–14:00</option><option>16:00–18:00</option></select></label></div><div className="grid gap-4 sm:grid-cols-2"><label><span className="mb-2 block text-sm font-semibold">Items</span><input type="number" min="1" value={quantity} onChange={e=>setQuantity(Number(e.target.value))} className="w-full rounded-xl border border-line bg-cream px-4 py-3 outline-none focus:border-leaf"/></label><label><span className="mb-2 block text-sm font-semibold">Contact number</span><div className="relative"><Phone className="absolute left-3 top-3.5 text-slate-400" size={17}/><input value={contact} onChange={e=>setContact(e.target.value)} className="w-full rounded-xl border border-line bg-cream px-10 py-3 outline-none focus:border-leaf" placeholder="+91..."/></div></label></div><Button className="w-full py-3.5" onClick={()=>onBooked({address,date,time,quantity,contact})}>Confirm pickup <ArrowRight size={16}/></Button></CardContent></Card><Card className="h-fit bg-[#edf6ef] shadow-none"><CardContent><p className="text-xs font-bold uppercase tracking-[.16em] text-leaf">Selected recycler</p><div className="mt-4 flex items-start gap-3"><div className="grid h-10 w-10 place-items-center rounded-xl bg-white text-leaf"><CheckCircle2 size={19}/></div><div><p className="font-semibold">{recycler.name}</p><p className="mt-1 text-sm leading-6 text-slate-500">{recycler.location}</p></div></div><div className="mt-6 border-t border-leaf/15 pt-4 text-sm"><p className="font-semibold">What happens next?</p><p className="mt-2 leading-6 text-slate-600">Your pickup will appear in My Pickups, where you can follow each handoff milestone.</p></div></CardContent></Card></div></div>}