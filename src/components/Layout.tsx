import {LayoutDashboard,Leaf,LogOut,Menu,PackageCheck,Plus,Recycle,Settings,Truck,X} from 'lucide-react'; import {useState} from 'react'; import type {ReactNode} from 'react'; import {Button} from './ui/Button';
export type Page='landing'|'signin'|'signup'|'dashboard'|'add'|'pickups'|'impact'|'analysis'|'recyclers'|'schedule'|'track'|'certificate'|'profile'|'recycler';
const navItems=[['dashboard','Dashboard',LayoutDashboard],['add','Add E-Waste',Plus],['pickups','My Pickups',PackageCheck],['impact','My Impact',Leaf],['profile','Profile / Settings',Settings],['recycler','Recycler portal',Truck]] as const;
export function Navbar({page,setPage}:{page:Page;setPage:(p:Page)=>void}){const [open,setOpen]=useState(false);return <header className="sticky top-0 z-20 border-b border-line/80 bg-cream/90 backdrop-blur md:hidden"><div className="flex items-center justify-between px-5 py-3"><button onClick={()=>setPage('dashboard')} className="flex items-center gap-2 font-bold"><span className="grid h-9 w-9 place-items-center rounded-xl bg-ink text-white"><Recycle size={18}/></span>EcoLoop</button><div className="flex items-center gap-2"><Button onClick={()=>setPage('add')}><Plus size={15}/>Recycle</Button><button aria-label="Toggle navigation" onClick={()=>setOpen(!open)} className="grid h-10 w-10 place-items-center rounded-xl border border-line bg-white">{open?<X size={18}/>:<Menu size={18}/>}</button></div></div>{open&&<nav className="border-t border-line/80 px-5 py-3"><div className="grid gap-1">{navItems.map(([id,label,Icon])=><button key={id} onClick={()=>{setOpen(false);setPage(id)}} className={`flex items-center gap-3 rounded-xl px-3 py-3 text-left text-sm font-medium ${page===id?'bg-white':'text-slate-600'}`}><Icon size={17}/>{label}</button>)}</div></nav>}</header>}
export function Sidebar({page,setPage}:{page:Page;setPage:(p:Page)=>void}){return <aside className="hidden w-64 shrink-0 border-r border-line/80 bg-cream md:block"><div className="sticky top-0 flex h-screen flex-col p-5"><button onClick={()=>setPage('dashboard')} className="mb-10 flex items-center gap-2 text-left font-bold tracking-tight"><span className="grid h-9 w-9 place-items-center rounded-xl bg-ink text-white"><Recycle size={18}/></span>EcoLoop</button><p className="mb-3 px-3 text-[10px] font-bold uppercase tracking-[.18em] text-slate-400">Workspace</p><nav className="space-y-1">{navItems.map(([id,label,Icon])=><button key={id} onClick={()=>setPage(id)} className={`flex w-full items-center gap-3 rounded-xl px-3 py-2.5 text-sm font-medium transition ${page===id?'bg-white text-ink shadow-sm':'text-slate-600 hover:bg-white/70 hover:text-ink'}`}><Icon size={17}/>{label}</button>)}</nav><div className="mt-auto border-t border-line pt-4"><button onClick={()=>setPage('landing')} className="flex w-full items-center gap-3 rounded-xl px-3 py-2.5 text-sm font-medium text-slate-600 hover:bg-white/70"><LogOut size={17}/>Exit workspace</button></div></div></aside>}
export function Layout({page,setPage,children}:{page:Page;setPage:(p:Page)=>void;children:ReactNode}){return <div className="min-h-screen bg-cream"><div className="flex min-h-screen"><Sidebar page={page} setPage={setPage}/><div className="min-w-0 flex-1"><Navbar page={page} setPage={setPage}/><header className="hidden border-b border-line/80 bg-cream/80 md:block"><div className="flex items-center justify-between px-8 py-4"><p className="text-xs font-semibold uppercase tracking-[.16em] text-slate-400">EcoLoop workspace</p><button onClick={()=>setPage('profile')} className="rounded-lg px-3 py-2 text-sm font-semibold hover:bg-white">Profile</button></div></header><main className="mx-auto max-w-6xl px-5 py-8 md:px-8 md:py-10">{children}</main></div></div></div>}
export const AppLayout=Layout;
