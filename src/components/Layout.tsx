import {LayoutDashboard,Leaf,LogOut,PackageCheck,Plus,Recycle,Settings} from 'lucide-react'; import type {ReactNode} from 'react'; import {Button} from './ui/Button';
export type Page='landing'|'signin'|'signup'|'dashboard'|'add'|'pickups'|'impact'|'analysis'|'recyclers'|'schedule'|'track'|'certificate'|'profile';
const navItems=[['dashboard','Dashboard',LayoutDashboard],['add','Add E-Waste',Plus],['pickups','My Pickups',PackageCheck],['impact','My Impact',Leaf],['profile','Profile / Settings',Settings]] as const;
export function Navbar({page,setPage}:{page:Page;setPage:(p:Page)=>void}){return <header className="sticky top-0 z-20 border-b border-line bg-cream/95 md:hidden"><div className="flex items-center justify-between px-5 py-4"><button onClick={()=>setPage('dashboard')} className="flex items-center gap-2 font-bold"><span className="grid h-9 w-9 place-items-center rounded-xl bg-ink text-white"><Recycle size={19}/></span>EcoLoop</button><Button onClick={()=>setPage('add')}><Plus size={16}/>Recycle</Button></div></header>}
export function Sidebar({page,setPage}:{page:Page;setPage:(p:Page)=>void}){return <aside className="hidden w-60 shrink-0 border-r border-line bg-cream md:block"><div className="sticky top-0 flex h-screen flex-col p-5"><button onClick={()=>setPage('dashboard')} className="mb-8 flex items-center gap-2 text-left font-bold tracking-tight"><span className="grid h-9 w-9 place-items-center rounded-xl bg-ink text-white"><Recycle size={19}/></span>EcoLoop</button><nav className="space-y-1">{navItems.map(([id,label,Icon])=><button key={id} onClick={()=>setPage(id)} className={`flex w-full items-center gap-3 rounded-lg px-3 py-2.5 text-sm font-medium ${page===id?'bg-white shadow-sm':'text-slate-600 hover:bg-white/60'}`}><Icon size={17}/>{label}</button>)}</nav><div className="mt-auto border-t border-line pt-4"><button onClick={()=>setPage('landing')} className="flex w-full items-center gap-3 rounded-lg px-3 py-2.5 text-sm font-medium text-slate-600 hover:bg-white/60"><LogOut size={17}/>Exit workspace</button></div></div></aside>}
export function Layout({page,setPage,children}:{page:Page;setPage:(p:Page)=>void;children:ReactNode}){return <div className="min-h-screen bg-cream"><div className="flex min-h-screen"><Sidebar page={page} setPage={setPage}/><div className="min-w-0 flex-1"><Navbar page={page} setPage={setPage}/><header className="hidden border-b border-line bg-cream/95 md:block"><div className="flex items-center justify-between px-8 py-4"><p className="text-sm text-slate-500">EcoLoop workspace</p><button onClick={()=>setPage('profile')} className="text-sm font-semibold">Profile</button></div></header><main className="mx-auto max-w-6xl px-5 py-8 md:px-8">{children}</main></div></div></div>}
export const AppLayout=Layout;
