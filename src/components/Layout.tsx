import {Recycle,LayoutDashboard,PackageCheck,Leaf,Plus} from 'lucide-react'; import type {ReactNode} from 'react'; import {Button} from './ui/Button';
export type Page='dashboard'|'add'|'pickups'|'impact'|'analysis'|'recyclers'|'schedule'|'track'|'certificate';
export function Layout({page,setPage,children}:{page:Page;setPage:(p:Page)=>void;children:ReactNode}){return <div className="min-h-screen bg-cream"><header className="sticky top-0 z-20 border-b border-line/80 bg-cream/90 backdrop-blur"><div className="mx-auto flex max-w-6xl items-center justify-between px-5 py-4"><button onClick={()=>setPage('dashboard')} className="flex items-center gap-2 font-bold tracking-tight"><span className="grid h-9 w-9 place-items-center rounded-xl bg-ink text-white"><Recycle size={19}/></span>EcoLoop</button><nav className="hidden items-center gap-1 md:flex">{[['dashboard','Dashboard',LayoutDashboard],['add','Recycle an Item',Plus],['pickups','My Pickups',PackageCheck],['impact','My Impact',Leaf]].map(([id,label,Icon]:any)=><button key={id} onClick={()=>setPage(id)} className={`flex items-center gap-2 rounded-lg px-3 py-2 text-sm font-medium ${page===id?'bg-white shadow-sm':'text-slate-600 hover:bg-white/60'}`}><Icon size={16}/>{label}</button>)}</nav><Button className="md:hidden" onClick={()=>setPage('add')}><Plus size={16}/>Recycle</Button></div></header><main className="mx-auto max-w-6xl px-5 py-8">{children}</main></div>}
