import type {ButtonHTMLAttributes} from 'react'; import {cn} from '../../lib/utils';
export function Button({className='',variant='primary',...p}:ButtonHTMLAttributes<HTMLButtonElement>&{variant?:'primary'|'secondary'|'ghost'|'danger'}){return <button className={cn('inline-flex items-center justify-center gap-2 rounded-xl px-4 py-2.5 text-sm font-semibold transition active:scale-[.98] disabled:opacity-50',variant==='primary'&&'bg-ink text-white hover:bg-[#1a3a2d]',variant==='secondary'&&'bg-mint text-ink hover:bg-[#d0eadb]',variant==='ghost'&&'bg-transparent text-ink hover:bg-white',variant==='danger'&&'bg-red-50 text-red-700',className)} {...p}/>}
